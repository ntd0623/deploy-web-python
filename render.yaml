services:
  - type: web
    name: flask-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn website:create_app
    plan: free
    envVars:
      - key: FLASK_ENV
        value: production
